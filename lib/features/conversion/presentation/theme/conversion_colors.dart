import 'package:flutter/material.dart';

/// Brand and layout colors aligned with the interview mockups.
abstract final class ConversionColors {
  static const Color scaffoldBackground = Color(0xFFE8F4FC);
  static const Color accent = Color(0xFFF5B320);
  static const Color accentDark = Color(0xFFE6A010);
  static const Color card = Colors.white;
  static const Color summaryText = Color(0xFF6B7280);
  static const Color blob = Color(0xFFF5B320);

  /// Currency codes (TENGO/QUIERO chips, amount prefix) and typed amount digits.
  static const Color amountValue = Color(0xFF000000);
  static const Color chevron = Color(0xFF000000);
}
